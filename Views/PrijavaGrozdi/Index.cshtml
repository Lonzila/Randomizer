@model IEnumerable<Randomizer.Models.PrijavaGrozdi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Prijava)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Grozd)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Prijava.PrijavaID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Grozd.GrozdID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PrijavaGrozdiID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PrijavaGrozdiID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PrijavaGrozdiID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
